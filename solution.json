{
  "requires":{
    "requires 1":"use transaction OKB9 and remove the G/L account for that company code",
    "requires 2":"use tcode FS00 and untick post automatically from control tab",
    "requires 3":"use tcode FINSC_LEDGER",
    "requires 4":"use Tcode ob52 to put those periods",
    "requires 5": "use SPRO to customize the cross application component and also assign the number ranges both from BP to vendors",
    "requires 6":"Go to system in SAP easy access and go to user parameters and remove the default parameter BUK"
  }
}
